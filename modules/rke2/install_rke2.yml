---
- name: Deploy RKE2 on Payload cluster
  hosts: k8s_cluster
  become: true
  vars_files:
    - "vars/{{ ENVIRONNEMENT }}/variables.yaml"
    - "vars/{{ ENVIRONNEMENT }}/secrets.yaml"

  tasks: 
    - ansible.builtin.import_role:
        name: nsfabrice2009.config.ansible_role_rke2
